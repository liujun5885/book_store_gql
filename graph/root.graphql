schema {
    query: RootQuery
    mutation: RootMutation
}

type RootQuery
type RootMutation

scalar Time

type PageInfo {
    totalItems: Int!
    totalPages: Int!
    page: Int!
    pageSize: Int!
}

input PageCursor {
    page: Int!
    pageSize: Int!
}
